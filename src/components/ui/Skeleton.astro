---
interface Props {
  class?: string;
  [key: string]: any;
}

const { class: className = '', ...rest } = Astro.props;

const classes = ["animate-pulse rounded-md bg-muted/70", className].join(' ');
---

<div class={classes} {...rest}></div>
